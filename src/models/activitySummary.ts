export type ActivitySummary = {
    id: string;

    area: string;
    distance: number;
    averageSpeed: number;
    elevation: number;
    maxSpeed: number;
    comments: number;
    
    timestamp: number;
};
