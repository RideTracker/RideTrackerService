{
  "name": "test-service",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "@types/itty-router-extras": "^0.4.0",
    "better-sqlite3": "^8.0.1",
    "typescript": "^5.0.3",
    "wrangler": "2.14.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "database": "wrangler d1 execute staging --local --persist-to local --file=./schema.sql",
    "deploy:database": "wrangler d1 execute staging --file ./schema.sql"
  },
  "dependencies": {
    "@cfworker/json-schema": "^1.12.5",
    "bcryptjs": "^2.4.3",
    "itty-router": "^3.0.12",
    "itty-router-extras": "^0.4.2"
  }
}
